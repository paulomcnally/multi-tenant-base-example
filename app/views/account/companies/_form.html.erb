<%= form_with(model: account_company, local: true) do |form| %>
  <% if account_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_company.errors.count, "error") %> prohibited this account_company from being saved:</h2>

      <ul>
        <% account_company.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number %>
  </div>

  <div class="field">
    <%= form.label :enabled %>
    <%= form.check_box :enabled %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
